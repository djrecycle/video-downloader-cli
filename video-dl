#!/usr/bin/env sh
set -eu

ROOT=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)

if [ -x "$ROOT/.venv/bin/python" ]; then
  exec "$ROOT/.venv/bin/python" "$ROOT/src/downloader/cli.py" "$@"
fi

exec python3 "$ROOT/src/downloader/cli.py" "$@"
